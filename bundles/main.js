/*
  This is the base for all assets in the theme
*/

// Opt-in to hot replacement
if (module.hot) module.hot.accept();

/** CSS */
import '../sass/style.sass';

/** JS */
import '../javascript/src/main.js';
